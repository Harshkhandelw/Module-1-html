1. Are the HTML tags and elements the same thing?
Ans:-
     Html tags and elements are different. html tags are start with < and And with >.html elements are hold the content that html element.

2. What are tags and attributes in HTML?
Ans:-
     Html tags are used to  hold a html elements and tags are starting with < and And with >. 
attributes are used to describe the character of html element. htlml attribute are found in starting of a html tags.
     
3. What are void elements in HTML?
Ans:-
     That tags does not have a And tag only have a starting tags are called void element. Ex:- input tag,img tag.
     
4. What are HTML Entities?
Ans:-
     Html entities are used to create a symbol for a character are called Entities. Ex:-&nbsp;,&glt;.

5. What are different types of lists in HTML?
Ans:-
     Thare are there type of list in html following:-
     1.Unordered list(ul):-The ordered list displays elements in numbered format.
     2.Ordered list(ol):-The unordered list displays elements in bulleted format
     3.description list(dl)

6. What is the ‘class’ attribute in HTML?
Ans:-
     Tha class attribute are used to point class name in css and Javascript.
  
7. What is the difference between the ‘id’ attribute and the ‘class’ attribute of HTML elements?
Ans:-
     The class attribute are used multiple time in multiple html elements. id attribute are used in a single time in a page.
     
8. What are the various formatting tags in HTML?
Ans:-
     The formatting tags are used to design a spacial type of taxt.
     
9. How is Cell Padding different from Cell Spacing?
Ans:-
     The space between wall and content are call cell padding.
     The space between two cell are called cell spacing.
     
10. How can we club two or more rows or columns into a single row or column in an HTML table?
Ans:-
      we can use rowspan to club two or more rows.
     we can use a colspan to club into a single row and column in html table.

11. What is the difference between a block-level element and an inline element?
Ans:-
     A block-level element don't allow to use his space in his area.
     A inline-elements allows to other to use his rast of space.
 
12. How to create a Hyperlink in HTML?
Ans:-
     we can create a hyperlink using a <a> tag. that connect one page to another.
     
13. What is the use of an iframe tag?
Ans:-
     The iframe use to load a another page in the document.

14. What is the use of a span tag? Explain with example?
Ans:-
     In use of span tag we can add any tag in the taxt without creating a new line.

15. How to insert a picture into a background image of a web page?
Ans:-
     The insert a picture in background of using a image url and image location and using "img" tag.

16. How are active links different from normal links?
Ans:-
     Active link are that link currently function as a lead link to open web page.
     Normal link are that link those refer link whether thay functional or not.

17. What are the different tags to separate sections of text?
Ans:-
     That are diffrant tags to use in a separate the taxt.
     Ex:- <br>,<p>
18. What is SVG?
Ans:-
     
19. What is difference between HTML and XHTML?
Ans:-     
     XHTML has a stricter syntax compared to html.
     HTML has a easier syntax.
     XHTML is a too expancive
     html is a no case-sensitive language.
     xhtml has only allow a lowercase tags only. 
     
           
20. What are logical and physical tags in HTML?
Ans:-
     Logical tags are used to provide a visual appearance in taxt.
     Physical tags are used to  add some logical or semantic value to the text.



























